# Railway configuration for EventFinder
# This file helps Railway understand your monorepo structure

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = ""
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
