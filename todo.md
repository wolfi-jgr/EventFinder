-> integration for real searches -> personalize!!
-> filter (date range, category, price)
-> sorting options (by date, distance, price) maybe radius slider
-> login prompt with process (+Security, Roles, Users)
-> real web scraping (HTML paring with JSoup)
-> UI Overhaul (add a map view (Google Maps or OpenStreetMap), mobile design, event page)
-> data management (deduplication, stale events, scheduled update jobs?)

-> API Enhancements (Pagination for large result sets, Better error responses: HTTP codes, API rate limiting, Request validation)
-> User Authentication (if needed) User registration/login, Save favorite events/places, User preferences (preferred categories, cities)
-> Caching Layer (Redis for frequently accessed data, Cache search results)

-> Testing
-> Documentation
-> Production Readiness (Environment configuration (dev/prod), Logging strategy, Monitoring/health checks, CI/CD pipeline)

PWA implementation