server:
  port: 8080

spring:
  config:
    import: optional:classpath:scrape-rules.yml
  datasource:
    # Prefer an explicit Spring JDBC URL set in `SPRING_DATASOURCE_URL`.
    # If you deploy to platforms that provide a JDBC URL, set it there.
    # Optionally `JDBC_DATABASE_URL` can be used as a secondary fallback.
    # NOTE: If your provider supplies a DATABASE_URL like
    # "postgres://user:pass@host:5432/db" convert it to a JDBC URL by
    # replacing the scheme with "jdbc:postgresql://" before setting it
    # in `SPRING_DATASOURCE_URL` (or set `JDBC_DATABASE_URL` accordingly).
    url: ${SPRING_DATASOURCE_URL:${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/eventfinder}}
    username: ${SPRING_DATASOURCE_USERNAME:eventfinder}
    password: ${SPRING_DATASOURCE_PASSWORD:eventfinder}
  jpa:
    hibernate:
      ddl-auto: validate  # Changed to validate since Flyway manages schema
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  jackson:
    serialization:
      write-dates-as-timestamps: false
    date-format: yyyy-MM-dd'T'HH:mm:ss
    time-zone: Europe/Vienna

app:
  cors-origins: ${APP_CORS_ORIGINS:http://localhost:5173}
  events:
    # Categories treated as club nights (visible until next day 06:00 by backend rule)
    club-categories: ${APP_EVENTS_CLUB_CATEGORIES:PARTY,CLUB}
    cleanup:
      # Daily cleanup time (Spring cron: second minute hour day month weekday)
      cron: ${APP_EVENTS_CLEANUP_CRON:0 15 6 * * *}
      zone: ${APP_EVENTS_CLEANUP_ZONE:Europe/Vienna}
      # Keep expired events for N extra days before physical delete (0 = delete immediately when expired)
      retention-days: ${APP_EVENTS_CLEANUP_RETENTION_DAYS:0}

# Web Scraper Configuration
scraper:
  enabled: true
  min-delay-between-requests: 2000  # 2 seconds
  max-delay-between-requests: 5000  # 5 seconds
  connection-timeout: 10000         # 10 seconds
  max-retries: 3
  max-concurrent-scrapers: 3
  follow-redirects: true
  user-agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36"
  html-storage-path: ./scraper-data

